{
  "name": "backend",
  "version": "1.0.0",
  "description": "1.  2. created `models`,`routes` and `services` folders 3. add `.env` file to root folder 4. add `.gitignore` file to root folder and add `.env` and `/node_modules`",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongodb": "^6.3.0",
    "mongoose": "^8.0.3",
    "nodemon": "^3.0.2"
  }
}
